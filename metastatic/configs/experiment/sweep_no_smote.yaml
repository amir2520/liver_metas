# @package _global_

single_model:
  pipeline:
    resampler_layer: null
    model_layer:
      class_weight: null

hydra:
  sweeper:
    params:
      single_model.pipeline.model_layer.C: 0.1,1.0,10.0